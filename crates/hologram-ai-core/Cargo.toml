[package]
name        = "hologram-ai-core"
description = "Portable modality-neutral inference schemas, requests, completions, and stable errors for hologram-ai."
version.workspace     = true
edition.workspace     = true
rust-version.workspace = true
license.workspace     = true
repository.workspace  = true

[features]
default = ["std"]
std = []

[dependencies]
